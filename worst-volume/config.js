/* 共有ランキングの保存先。
   Google Apps Script の Web アプリ URL（https://script.google.com/macros/s/～/exec）を入れる。
   空のままなら、ランキングはこの端末（localStorage）だけに保存される。
   設定手順は RANKING.md を参照。 */
window.YASUINE_RANKING_URL = "";
